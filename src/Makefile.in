OCAMLBUILD=@OCAMLBUILD@ -classic-display 

pkind:
	$(OCAMLBUILD) pkind_main.native
	$(OCAMLBUILD) pkind.native

kind-invariant:
	$(OCAMLBUILD) kind_invariant.native

lsimplify:
	$(OCAMLBUILD) lsimplify_main.native

clean:
	$(OCAMLBUILD) -clean
